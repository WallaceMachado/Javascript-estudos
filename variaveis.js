// Variáveis

/**
 * Nome simbólicos para receber algum valor
 * Atalhso de código
 * Identificadores
 * 3 paralavras reservadas para criar uma variávels
    * var
    * let
    * const não pode ser alterada
 */

// scope escopo da varável

/**
 * determina a visibilidade de alguma variável
 * var funciona tanto no escopo global quanto no local, elevação hoisnten
 * let e const s´~ao locais e só funcionam no scopo onde foram criadas
 * 
 * escopo é tudo que está dentro de {  }
 */